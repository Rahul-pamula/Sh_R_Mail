==============================================================
  EMAIL ENGINE — STARTUP + TESTING GUIDE (MICROSERVICES)
==============================================================

NOTE: The Python virtual environment lives at the root:
  /Users/rahul/Desktop/Sh_R_Mail/.venv

  If it doesn't exist yet, create it once:
    cd /Users/rahul/Desktop/Sh_R_Mail
    python3 -m venv .venv
    source .venv/bin/activate
    pip install -r platform/api/requirements.txt

--------------------------------------------------------------
STEP 0: START ALL SERVICES
--------------------------------------------------------------

Tracking API → Supabase Edge Functions (always on, no tunnel needed)
  URL: https://iiweorjzoxcfaedsubxu.supabase.co/functions/v1
  API_URL in .env is already set — nothing to start!

Terminal 1 (API Server)
cd /Users/rahul/Desktop/Sh_R_Mail
source .venv/bin/activate
cd platform/api
uvicorn main:app --reload --host 0.0.0.0 --port 8000


Terminal 2 (Email Sender Worker)
  cd /Users/rahul/Desktop/Sh_R_Mail
  source .venv/bin/activate
  # Tune throughput locally:
  #   WORKER_PREFETCH_COUNT=10 → pull 10 messages at a time (faster)
  #   WORKER_MAX_RETRIES=3     → retry each message up to 3 times
  WORKER_PREFETCH_COUNT=10 WORKER_MAX_RETRIES=3 python platform/worker/email_sender.py

Terminal 3 (Dedicated Contact Import Worker — REQUIRED FOR IMPORTS)
  Contact imports now use a robust Direct-to-S3 streaming pipeline.
  This worker MUST be running or your imported files will be marked "pending" forever.
  cd /Users/rahul/Desktop/Sh_R_Mail
  source .venv/bin/activate
  python platform/worker/import_worker.py

Terminal 4 (Scheduler)
  cd /Users/rahul/Desktop/Sh_R_Mail
  source .venv/bin/activate
  python platform/worker/scheduler.py

Terminal 5 (Frontend)
  cd /Users/rahul/Desktop/Sh_R_Mail/platform/client
  npm run dev

Terminal 6 (Centralized System Email Worker)
  cd /Users/rahul/Desktop/Sh_R_Mail
  source .venv/bin/activate
  python platform/worker/centralized_email_worker.py

Open browser → http://localhost:3000

--------------------------------------------------------------
QUICK REFERENCE
--------------------------------------------------------------
Email worker logs       → Terminal 2 (email_sender.py)
Import worker logs      → Terminal 3 (import_worker.py)
Notification emails     → Your inbox
Rate limit responses    → curl output (look for 429)
DB state changes        → Supabase SQL Editor
Open tracking issues    → Check tunnel is running + "Display images" clicked in Gmail

--------------------------------------------------------------
KNOWN ISSUES / GOTCHAS
--------------------------------------------------------------

[NEW 2026-04-09] Next.js 16 proxy rename + clean build
  - The framework renamed middleware → proxy. Keep the file at:
      /Users/rahul/Desktop/Sh_R_Mail/platform/client/src/proxy.ts
  - If you still see the "middleware deprecated" warning, nuke the cache once:
      rm -rf /Users/rahul/Desktop/Sh_R_Mail/platform/client/.next
      cd /Users/rahul/Desktop/Sh_R_Mail/platform/client && npm run dev

[NEW 2026-04-09] Contacts page data sources
  - The Contacts UI now lazy-loads and cancels in-flight requests to avoid
    "Load failed" browser errors while you navigate quickly.
  - If import batches disappear, refresh once; if still empty, ensure the API is
    running on http://localhost:8000 and that your .env FRONTEND_URL matches.

[FIXED] Contacts Architecture (2026-04-21)
  - Contacts are no longer imported directly through the API server memory space.
  - The UI uploads files securely to Supabase S3 via a Presigned URL ticket.
  - The Dedicated Import Worker (`import_worker.py`) is notified via RabbitMQ,
    streams the file in chunks from S3, and bulk-upserts to the database.
